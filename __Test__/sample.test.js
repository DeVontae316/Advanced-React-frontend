describe('I need a global',()=>{
    xit('I need this to test',function test(){
        var age = 100;
        expect(age).toEqual(99);
        expect(1).toEqual(1);
    });

    /*test('List of dog names',()=>{
        let dogs = ['Bingo','Sammy','Bob'];

        expect(dogs).toContain('Bingo');
        expect(dogs).toEqual(dogs);
    })*/
});